const upperCase = str => str.toUpperCase();

const firstLetter = str => str.charAt(0);

const joinStrings = (str1, str2) => str1.concat(str2);

module.exports = {
  upperCase,
  firstLetter,
  joinStrings
}
